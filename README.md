# AI Telegram Bot

Telegram-бот агентства [1ma.ai](https://1ma.ai), который проводит диагностику бизнеса, обучает базовым принципам внедрения ИИ и формирует персональный PDF-отчёт с рекомендациями. После выдачи отчёта бот продолжает консультировать пользователя в формате диалога.

## Возможности
- Полностью русскоязычный сценарий, использующий точные тексты из брифа.
- Квалификация пользователя по уровню знаний + мини-обучение для новичков.
- 17 вопросов диагностики (15 о бизнесе + 2 о готовности) с мультивыбором и открытыми ответами.
- Сохранение ответов в Google Sheets для последующей аналитики.
- Генерация PDF-отчёта с гибридным содержимым: статические инструкции + рекомендации GPT-5-mini.
- Отложенное follow-up сообщение и консультационная кнопка.
- Пост-отчётное диалоговое окно на базе GPT-5-mini, учитывающее ответы пользователя и выданные рекомендации.

## Требования
- Python 3.11+
- Аккаунт Telegram и токен бота
- OpenAI API-ключ с доступом к модели `gpt-5-mini`
- Google аккаунт с доступом к Google Sheets + Apps Script (Web App)
- Аккаунт Vercel (для production-развёртывания)

## Установка

1. **Клонируйте репозиторий и установите зависимости**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```

2. **Настройте переменные окружения**
   Скопируйте шаблон:
   ```bash
   cp .env.example .env
   ```

   Заполните значения:

   | Переменная | Описание |
   |------------|----------|
   | `TELEGRAM_BOT_TOKEN` | Токен бота, выданный BotFather |
   | `TELEGRAM_SECRET_TOKEN` | (опционально) секрет для защиты вебхука; укажите то же значение при настройке вебхука |
   | `OPENAI_API_KEY` | OpenAI API ключ |
   | `OPENAI_MODEL` | Модель OpenAI (по умолчанию `gpt-5-mini`) |
   | `GOOGLE_CREDENTIALS_JSON` | (опционально) JSON сервисного аккаунта, если хотите писать напрямую через Sheets API |
   | `GOOGLE_SHEET_ID` | ID таблицы (строка между `/d/` и `/edit`) |
   | `GOOGLE_SHEET_RANGE` | Диапазон для записи (например `Ответы!A:Z`) |
   | `GAS_ENDPOINT` | URL опубликованного Google Apps Script Web App (`https://script.google.com/macros/s/.../exec`) |
   | `PDF_FONT_PATH` | (опционально) путь к TTF-шрифту с поддержкой кириллицы (например, DejaVuSans.ttf) |
   | `CONSULTATION_URL` | Ссылка на страницу бронирования консультации |
   | `WEBHOOK_URL` | Публичный URL вашего вебхука `https://<project>.vercel.app/api/webhook` |

3. **Google Apps Script Web App**
   - Откройте [Google Apps Script](https://script.google.com/), создайте проект и вставьте скрипт из раздела «Пример Apps Script» ниже (или используйте свой вариант).
   - Укажите ID вашей таблицы (`GOOGLE_SHEET_ID`) и лист (по умолчанию `Лист1`).
   - В меню **Project settings** включите отображение `appsscript.json` и вставьте:
     ```json
     {
       "timeZone": "Europe/Minsk",
       "dependencies": {},
       "exceptionLogging": "STACKDRIVER",
       "runtimeVersion": "V8",
       "webapp": {
         "executeAs": "USER_DEPLOYING",
         "access": "ANYONE_ANONYMOUS"
       }
     }
     ```
   - Через **Deploy → New deployment → Web app** установите доступ «Anyone», скопируйте URL (`.../exec`) и впишите его в переменную `GAS_ENDPOINT`.
   - (Опционально) если хотите использовать прямое подключение через Sheets API, настройте сервисный аккаунт и заполните `GOOGLE_CREDENTIALS_JSON`.

4. **Подготовьте PDF-шрифт (опционально)**
   ReportLab не умеет работать с кириллицей в стандартных шрифтах. Скачайте, например, [DejaVu Sans](https://dejavu-fonts.github.io/) и укажите путь в `PDF_FONT_PATH`. Если путь пустой, будет использован Helvetica (кириллица отображаться не будет).

5. **Настройте Telegram webhook**
   - После деплоя (или при локальном тесте через ngrok) выполните:
     ```bash
     python scripts/set_webhook.py set --url https://<project>.vercel.app/api/webhook
     ```
   - Для очистки:
     ```bash
     python scripts/set_webhook.py delete
     ```
   Скрипт читает токен и секрет из `.env`. Если указать `TELEGRAM_SECRET_TOKEN`, Telegram будет присылать его в заголовке `X-Telegram-Bot-Api-Secret-Token`, что проверяется внутри `api/webhook.py`.

## Развёртывание на Vercel

1. **Подготовьте GitHub-репозиторий** и загрузите код (Vercel подтягивает изменения из Git).
2. **Создайте новый проект в Vercel**, укажите корень репозитория.
3. **В разделе Environment Variables** добавьте значения из `.env` (минимум: `TELEGRAM_BOT_TOKEN`, `OPENAI_API_KEY`, `GAS_ENDPOINT`, `GOOGLE_SHEET_ID`, `WEBHOOK_URL`).
4. После деплоя возьмите домен вида `https://<project>.vercel.app` и обновите `WEBHOOK_URL`.
5. Выполните `python scripts/set_webhook.py set --url https://<project>.vercel.app/api/webhook`.

> **Важно:** укажите `TELEGRAM_SECRET_TOKEN` в Vercel и при установке вебхука, чтобы Telegram подписывал запросы.

## Как обновить содержимое
- **Видео в обучающем блоке:** замените текст `(ссылка на видео)` в `bot/messages.py` на реальный URL (Telegram поддерживает предпросмотр YouTube/Vimeo).
- **Статический контент отчёта:** редактируйте `templates/report_static.py` — заголовок, вводные параграфы и чек-листы.
- **Кнопка консультации:** обновите `CONSULTATION_URL` в `.env` / Vercel.

## Тестирование
- Для локальных тестов используйте ngrok или другой туннель, чтобы Telegram мог доставлять вебхуки.
- Проверьте, что `GAS_ENDPOINT` отвечает `{"ok": true}` при тестовом POST (можно использовать `curl` или Postman).
- После прохождения анкеты в таблице должна появиться новая строка с полными формулировками вопросов.
- После получения отчёта отправьте несколько сообщений в чат и убедитесь, что GPT отвечает, опираясь на рекомендации.

## Структура проекта
```
api/webhook.py            # HTTP-эндпойнт для Vercel
bot/                      # Логика сценариев и обработчиков
services/                 # Интеграции OpenAI, Google Sheets, PDF
templates/report_static.py# Статическое содержимое отчёта
scripts/set_webhook.py    # Утилита для установки/удаления вебхука
vercel.json               # Конфигурация Vercel runtime
```

## Полезные команды
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск скрипта установки вебхука
python scripts/set_webhook.py set --url https://<project>.vercel.app/api/webhook

# Сброс вебхука
python scripts/set_webhook.py delete
```

## Дополнительно
- Чтобы обновить статическую таблицу ответов, можно подключить Google Data Studio / Looker Studio напрямую к Google Sheets ID.
- При необходимости добавьте в Vercel Cron или Scheduler для регулярных напоминаний пользователю — базовая структура уже поддерживает асинхронные задачи через Telegram API.
- Для мониторинга ошибок включите логирование Vercel (Request Logs) и/или добавьте Sentry, подключив его в `bot/handlers.py`.

Бот готов к деплою и дальнейшему расширению: добавляйте новые блоки диагностики, интегрируйте CRM или другие источники данных, обновляйте статический контент отчётов.

## Пример Apps Script

```javascript
function doPost(e) {
  try {
    var raw = e.postData && e.postData.contents ? e.postData.contents : '{}';
    var data = JSON.parse(raw);

    var SPREADSHEET_ID = 'ВАШ_ID_ТАБЛИЦЫ';
    var SHEET_NAME = 'Лист1';

    var ss = SpreadsheetApp.openById(SPREADSHEET_ID);
    var sh = ss.getSheetByName(SHEET_NAME) || ss.insertSheet(SHEET_NAME);

    var baseHeaders = [
      'Дата заполнения анкеты',
      'Telegram user_id',
      'Username',
      'Полное имя',
      'Выбранный уровень знаний в ИИ'
    ];

    var questionHeaders = [
      '💼 В какой сфере ты работаешь?',
      '💼 Расскажи подробнее, чем занимается твоя компания. Что вы продаёте и какие услуги оказываете?',
      '🌐 Поделись ссылкой на сайт или соцсети компании (например: Instagram, сайт или Telegram-канал)',
      '🚀 Какой сейчас основной канал привлечения клиентов?',
      '📋 Есть ли у вас клиентская база и насколько активно вы с ней работаете — допродажи, реанимация старых клиентов, рассылки и т. д.?',
      '🗣 Как часто вам приходится отвечать клиентам на одни и те же вопросы — про цену, наличие, доставку или запись?',
      '🎥 Много ли времени и денег команда тратит на создание контента — рилсы, креативы, описания товаров и т. д.?',
      '📊 Есть ли у вас сейчас единая система, где можно в реальном времени видеть ключевые показатели бизнеса — продажи, заявки, финансы, эффективность рекламы и т. д.?',
      '📊 Как часто вы нанимаете сотрудников и насколько трудозатратен процесс подбора, адаптации и контроля за сотрудниками?',
      '🗂️ Как часто вы работаете с документами (договора, счета, акты, ТТН), насколько этот процесс важен и трудозатратен?',
      '🧩 Где, по твоему ощущению, ИИ мог бы помочь в первую очередь твоему бизнесу? Возможно уже слышал про кейсы внедрения или есть идеи?',
      '😤 Есть ли процессы в компании, которые раздражают и хочется автоматизировать?',
      '👥 Сколько человек работает в твоей компании?',
      '🤔 Было бы полезно иметь ИИ-ассистента, который помогает сотрудникам в рутине (отчёты, тексты, общение, CRM)? Если да — расскажи, кому именно он мог бы пригодиться.',
      '🎯 Какая у тебя основная цель на ближайшие месяцы?',
      '🧠 Насколько на твой взгляд систематизирован твой бизнес? (1 — хаос, 5 — всё по регламентам)',
      '📦 Как у вас оформлен каталог товаров или услуг? Есть ли он в Excel, 1С, МойСклад, Notion, на сайте или в другом виде?'
    ];

    ensureHeaders(sh, baseHeaders.concat(questionHeaders));

    var meta = data.meta || {};
    var answers = data.answers_by_id || {};

    var row = [
      meta.timestamp || new Date().toISOString(),
      meta.user_id || '',
      meta.username || '',
      meta.full_name || '',
      meta.skill_level || ''
    ];

    questionHeaders.forEach(function (title, idx) {
      var questionId = data.answers && data.answers[idx] ? data.answers[idx].id : '';
      var value = questionId && answers[questionId] ? answers[questionId] : '';
      row.push(value);
    });

    sh.appendRow(row);

    return ContentService
      .createTextOutput(JSON.stringify({ ok: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({ ok: false, error: String(error) }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function ensureHeaders(sheet, headers) {
  if (sheet.getLastRow() === 0) {
    sheet.appendRow(headers);
    return;
  }
  var current = sheet.getRange(1, 1, 1, headers.length).getValues()[0];
  var needUpdate = headers.some(function (header, idx) {
    return current[idx] !== header;
  });
  if (needUpdate) {
    sheet.getRange(1, 1, 1, headers.length).setValues([headers]);
  }
}

function doGet() {
  return ContentService.createTextOutput('OK');
}
```
